# This directory is for Docker secrets in production
# Create the following files with your secrets:
#
# postgres_password.txt - PostgreSQL password
# jwt_secret.txt - JWT signing secret
# smtp_password.txt - SMTP password
#
# IMPORTANT: Never commit actual secret files to version control!
# Add the following to .gitignore:
# secrets/*.txt
# !secrets/.gitkeep
